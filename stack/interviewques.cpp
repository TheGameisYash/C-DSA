#include <iostream>
#include<stack>
using namespace std;



int main () {
    
    /* //reverse a string using a stack === LIFO order follow karta hai stack
    
    string str = "STARK";

    stack<char> s;

    for (int i=0; i<str.length(); i++){
        char ch = str[i];
        s.push(ch);
    }

    string ans = "";
    while(!s.empty()){
        char ch= s.top();
        ans.push_back(ch);

        s.pop();
    }

    cout << "answer is:  "<< ans << endl;
    */

   // delete middle element from stack

   


}
